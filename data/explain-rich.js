// Explications enrichies (paragraphes pédagogiques rédigés, fidèles à la doc
// officielle mais formulés ici — pas une reproduction). Surchargent le champ
// `explain` par id. Lot 1 : Architecture + Troubleshooting (les 2 plus gros poids).
// Ajouter d'autres domaines par lots dans ce même objet.
(function () {
  const RICH = {
    /* ---------- ARCHITECTURE (theory.js) ---------- */
    "th-arch-1": "etcd est une base clé-valeur distribuée et cohérente : c'est l'unique source de vérité du cluster, et seul l'API server y lit et y écrit. Comme toute la configuration et tout l'état des objets y résident, sauvegarder etcd (via un snapshot) revient à sauvegarder le cluster entier — d'où l'importance de savoir le faire et le restaurer au CKA.",
    "th-arch-2": "Le kube-scheduler surveille les Pods nouvellement créés qui n'ont pas encore de nœud. Pour chacun, il élimine d'abord les nœuds inadaptés (ressources insuffisantes, taints non tolérés, affinités non satisfaites), puis note les nœuds restants pour retenir le meilleur. Il n'exécute pas le Pod : il écrit seulement l'affectation, que le kubelet du nœud concerné met ensuite en œuvre.",
    "th-arch-3": "Chaque nœud worker exécute deux composants indispensables : le kubelet, qui dialogue avec l'API server et pilote le runtime pour faire tourner les Pods, et kube-proxy, qui programme les règles réseau (iptables/ipvs) rendant les Services joignables. Le scheduler et etcd, eux, appartiennent au control plane et ne tournent pas sur chaque worker.",
    "th-arch-4": "Ajouter un worker nécessite un token d'amorçage valide et le hash du certificat du CA. `kubeadm token create --print-join-command` génère un nouveau token et imprime la commande `kubeadm join` complète, prête à coller sur le futur nœud. C'est indispensable car le token affiché par `kubeadm init` expire par défaut au bout de 24 heures.",
    "th-arch-5": "Le kubelet surveille en continu le dossier /etc/kubernetes/manifests : tout manifeste de Pod qui s'y trouve est démarré directement par lui, sans passer par le scheduler — ce sont les Static Pods. C'est ainsi que kubeadm lance l'API server, etcd, le scheduler et le controller-manager ; ajouter ou retirer un fichier y crée ou supprime le Pod correspondant.",
    "th-arch-6": "La sauvegarde d'etcd utilise l'API v3 : `ETCDCTL_API=3 etcdctl snapshot save`, en fournissant l'endpoint et les certificats client (--cacert/--cert/--key). On peut vérifier l'intégrité du fichier avec `snapshot status`, puis restaurer avec `snapshot restore` dans un nouveau data-dir avant de repointer le Static Pod etcd dessus.",
    "th-arch-7": "Un ServiceAccount donne une identité aux processus qui tournent dans un Pod, pour qu'ils s'authentifient auprès de l'API server. À l'inverse, les utilisateurs humains ne sont pas des objets Kubernetes : ils sont gérés hors du cluster (certificats client, OIDC…). On rattache ensuite des droits à un ServiceAccount via RBAC.",
    "th-arch-8": "En RBAC, un Role définit des permissions dans un namespace, mais ne les accorde à personne tant qu'un RoleBinding ne le lie pas à un sujet (user, group ou ServiceAccount) de ce namespace. Un RoleBinding peut aussi référencer un ClusterRole pour en restreindre l'usage à un namespace. Pour des droits à l'échelle du cluster, on utilise plutôt un ClusterRoleBinding.",
    "th-arch-9": "L'ordre d'un upgrade kubeadm est strict : on met d'abord à niveau le control plane avec `kubeadm upgrade apply`, puis kubelet/kubectl du nœud de contrôle. On traite ensuite chaque worker un par un : drain pour l'évacuer, `kubeadm upgrade node`, mise à jour de kubelet, puis uncordon. Un worker ne doit jamais dépasser la version du control plane.",
    "th-arch-10": "Le kube-apiserver expose l'API REST et constitue le point d'entrée unique du cluster : kubectl, les contrôleurs, les kubelets et le scheduler passent tous par lui. C'est aussi le seul composant autorisé à lire et écrire dans etcd, ce qui en fait le véritable cœur de communication du cluster.",
    "th-arch-11": "`kubectl auth can-i <verbe> <ressource>` interroge le module d'autorisation pour savoir si une action serait permise, sans l'exécuter. Avec `--as` (impersonation), un administrateur teste les droits d'un autre utilisateur ou ServiceAccount. C'est l'outil de référence pour diagnostiquer un refus RBAC.",
    "th-arch-12": "kubeadm range l'infrastructure à clés du cluster dans /etc/kubernetes/pki : le CA (ca.crt/ca.key) et les certificats des composants. C'est à partir de ce CA que l'on régénère les certificats expirés — par exemple avec `kubeadm certs renew all` — avant de redémarrer les Static Pods concernés.",

    /* ---------- ARCHITECTURE (theory2.js) ---------- */
    "t2-a1": "etcd sépare deux flux réseau : le port 2379 pour les clients (dont l'API server), et le 2380 pour la communication entre membres etcd (peer). Retenir 2379 est utile pour cibler les commandes etcdctl, via --endpoints=https://127.0.0.1:2379.",
    "t2-a2": "Par défaut, le kube-apiserver sert son API sécurisée en HTTPS sur le port 6443. C'est l'adresse que l'on retrouve dans le kubeconfig (server: https://<ip>:6443) et vers laquelle pointent kubectl comme les autres composants.",
    "t2-a3": "Le kube-controller-manager regroupe de nombreux contrôleurs qui exécutent chacun une boucle de réconciliation : ils comparent l'état désiré (stocké dans etcd) à l'état réel et agissent pour les rapprocher. On y trouve par exemple les contrôleurs de nœuds, de réplication, d'endpoints ou de signature de certificats.",
    "t2-a4": "`kubectl api-resources` liste tous les types de ressources connus du cluster, avec leur nom court (po, deploy, svc…), leur groupe d'API et leur portée (namespacé ou non). Pratique pour retrouver un shortname ou vérifier qu'une CRD est bien enregistrée.",
    "t2-a5": "kubeadm configure l'API server avec deux authorizers en chaîne : Node, qui restreint ce que chaque kubelet peut lire ou écrire, et RBAC, qui applique les rôles définis. Une requête est autorisée dès que l'un des modules l'accepte.",
    "t2-a6": "Le ClusterRole intégré `cluster-admin` confère un accès total au cluster. On l'accorde en le liant à un sujet via un ClusterRoleBinding. À manier avec précaution : c'est le rôle le plus privilégié fourni par défaut.",
    "t2-a7": "`kubeadm init --pod-network-cidr` déclare la plage d'adresses réservée aux Pods. Cette valeur doit correspondre à ce qu'attend le plugin CNI installé ensuite (par exemple 10.244.0.0/16 pour Flannel), sinon le réseau des Pods ne fonctionnera pas.",
    "t2-a8": "Une CustomResourceDefinition enregistre un nouveau type de ressource (kind) auprès de l'API server, qui le sert alors comme une ressource native. C'est le mécanisme sur lequel reposent les opérateurs, comme la ressource Cluster de CloudNativePG.",
    "t2-a9": "Le token d'amorçage créé par kubeadm expire par défaut au bout de 24 heures, par sécurité. Passé ce délai, on en régénère un avec `kubeadm token create --print-join-command` pour ajouter de nouveaux workers.",
    "t2-a10": "`kubectl config set-context --current --namespace=<ns>` fixe le namespace par défaut du contexte courant. Les commandes suivantes s'y appliquent sans avoir à répéter `-n` — un gain de temps appréciable à l'examen.",
    "t2-a11": "L'approbation et la signature d'une CertificateSigningRequest sont deux étapes distinctes : une fois la CSR approuvée, c'est le contrôleur de signature intégré au kube-controller-manager qui émet le certificat à partir du CA du cluster.",
    "t2-a12": "kubeadm génère le kubeconfig administrateur dans /etc/kubernetes/admin.conf. Pour l'utiliser avec kubectl, on le copie vers ~/.kube/config. À ne pas confondre avec kubelet.conf, qui sert à l'authentification du kubelet.",
    "t2-a13": "Un ResourceQuota plafonne la consommation agrégée d'un namespace : total de CPU/mémoire demandé, mais aussi nombre d'objets (Pods, Services, PVC…). Il raisonne à l'échelle du namespace entier, là où un LimitRange borne chaque objet.",
    "t2-a14": "Un LimitRange agit au niveau de chaque conteneur ou Pod d'un namespace : il fixe des valeurs par défaut de requests/limits et des bornes min/max. Il complète le ResourceQuota, qui lui raisonne sur la somme du namespace.",
    "t2-a15": "Un PodDisruptionBudget protège la disponibilité pendant les disruptions volontaires, comme un `kubectl drain` : il garantit qu'un minimum de Pods reste disponible. Il évite ainsi de couper un service durant la maintenance des nœuds, mais ne protège pas des pannes involontaires (crash).",
    "t2-a16": "`kubeadm certs renew all` régénère tous les certificats gérés par kubeadm à partir du CA du cluster. On visualise d'abord les échéances avec `kubeadm certs check-expiration`, puis on redémarre les Static Pods concernés pour prendre en compte les nouveaux certificats.",
    "t2-a17": "Le comportement du kubelet installé par kubeadm est décrit dans /var/lib/kubelet/config.yaml. À ne pas confondre avec kubelet.conf, qui est le kubeconfig d'authentification du kubelet auprès de l'API server.",
    "t2-a18": "Pour accorder des droits à l'échelle du cluster, on crée un ClusterRoleBinding reliant un ClusterRole (ici cluster-admin) à un sujet. Un simple RoleBinding, lui, ne porterait que sur un namespace.",
    "t2-a19": "Sur le premier nœud du control plane, `kubeadm upgrade apply vX.Y.Z` met à niveau les composants (API server, controller-manager, scheduler…). La mise à jour de kubelet se fait ensuite séparément, et les workers sont traités après avec `kubeadm upgrade node`.",
    "t2-a20": "Le kubelet est l'agent présent sur chaque nœud : il reçoit les PodSpecs depuis l'API server et pilote le runtime de conteneurs via l'interface CRI pour les exécuter. C'est aussi lui qui rapporte l'état du nœud et le rend Ready.",
    "t2-a21": "CoreDNS assure la résolution DNS interne du cluster : il permet de joindre un Service par son nom (<svc>.<ns>.svc.cluster.local). Il est déployé comme add-on dans le namespace kube-system.",
    "t2-a22": "Le kubelet étant géré par systemd sur le nœud, `systemctl status kubelet` et `journalctl -u kubelet` sont les premiers réflexes face à un nœud NotReady (souci de configuration, de CNI ou de certificats).",

    /* ---------- TROUBLESHOOTING (theory.js) ---------- */
    "th-ts-1": "`kubectl logs <pod>` affiche la sortie d'un conteneur ; on cible avec `-c <container>` si le Pod en contient plusieurs. Après un crash, `--previous` (ou -p) lit les logs de l'instance précédente — souvent la clé pour comprendre pourquoi le conteneur s'est arrêté.",
    "th-ts-2": "CrashLoopBackOff signifie que le conteneur démarre, se termine (le plus souvent avec un code non nul), et que Kubernetes le relance en boucle avec un délai croissant (back-off). Le diagnostic passe par `kubectl logs --previous` pour voir l'erreur applicative et par les Events du `describe` pour le contexte.",
    "th-ts-3": "Un Pod Pending n'est pas encore planifié : il n'y a donc pas de logs applicatifs à consulter. La cause figure dans les Events de `kubectl describe pod`, typiquement un FailedScheduling (ressources insuffisantes, taints non tolérés, affinités non satisfaites).",
    "th-ts-4": "ImagePullBackOff indique que le kubelet n'arrive pas à télécharger l'image du conteneur. Les causes fréquentes : nom ou tag erroné, registre privé sans imagePullSecret, ou registre injoignable. Le message précis apparaît dans les Events du Pod.",
    "th-ts-5": "Un nœud NotReady vient le plus souvent du kubelet, chargé de signaler l'état du nœud. On vérifie donc en priorité `systemctl status kubelet` et ses logs, puis le plugin réseau (CNI) et les certificats. etcd et le scheduler, eux, sont côté control plane.",
    "th-ts-6": "`kubectl top pod` (et `top node`) affiche la consommation réelle de CPU et de mémoire. Ces commandes s'appuient sur le metrics-server : s'il n'est pas installé, elles échouent avec une erreur d'API metrics.",
    "th-ts-7": "L'erreur « connection to the server localhost:8080 was refused » signifie que kubectl n'a trouvé aucun kubeconfig valide et retombe sur l'ancienne adresse par défaut. On vérifie alors la variable KUBECONFIG et le fichier ~/.kube/config.",
    "th-ts-8": "`kubectl get events --sort-by=.metadata.creationTimestamp` trie les événements par date, ce qui permet de reconstituer la chronologie d'un incident dans un namespace. En ajoutant `-A`, on couvre tout le cluster.",
    "th-ts-9": "Quand l'image d'un conteneur n'a ni shell ni outils réseau, `kubectl debug` y injecte un conteneur éphémère qui partage les mêmes namespaces, sans redémarrer ni redéployer le Pod. On peut alors inspecter le process, le réseau ou le système de fichiers.",
    "th-ts-10": "`kubectl exec -it <pod> -- sh` ouvre un shell interactif dans un conteneur déjà en cours d'exécution (option `-c` pour le choisir). Contrairement à `kubectl run`, on n'entre pas dans un nouveau Pod mais dans le conteneur existant.",

    /* ---------- TROUBLESHOOTING (theory2.js) ---------- */
    "t2-t1": "Un Pod Pending n'a pas encore de nœud : il n'y a donc rien à lire dans les logs. Les Events de `kubectl describe pod` révèlent la cause côté scheduler, souvent un FailedScheduling avec un motif précis (ressources, taints, affinités).",
    "t2-t2": "Après un crash, les logs du conteneur actuel peuvent être vides ou ne montrer que le redémarrage. `kubectl logs <pod> --previous` lit ceux de l'instance morte, où se trouve généralement l'erreur réelle.",
    "t2-t3": "En CrashLoopBackOff, le conteneur démarre puis se termine en erreur, et Kubernetes le relance avec un délai croissant. Ce n'est pas un problème d'image (qui donnerait ImagePullBackOff) mais un problème d'exécution : commande, configuration ou dépendance manquante.",
    "t2-t4": "ImagePullBackOff est un problème de récupération d'image, pas d'exécution. On vérifie le nom et le tag de l'image, l'accès au registre, et — pour un registre privé — la présence d'un imagePullSecret.",
    "t2-t5": "Le kubelet est responsable de l'état Ready d'un nœud ; c'est donc le premier suspect quand un nœud passe NotReady. On inspecte son service (systemctl/journalctl), puis le CNI qui fournit le réseau des Pods.",
    "t2-t6": "`kubectl top pod` donne la consommation live de CPU/mémoire, indispensable pour repérer une surconsommation. Elle dépend du metrics-server : sans lui, la commande renvoie une erreur.",
    "t2-t7": "Trier les événements par date avec `--sort-by=.metadata.creationTimestamp` reconstitue l'enchaînement des faits lors d'un incident. C'est un réflexe de corrélation, à élargir à tout le cluster avec `-A`.",
    "t2-t8": "Quand aucun kubeconfig valide n'est chargé, kubectl tente l'ancienne adresse locale et échoue avec « localhost:8080 refused ». La correction consiste à pointer KUBECONFIG (ou ~/.kube/config) vers le bon fichier.",
    "t2-t9": "`kubectl exec -it <pod> -- sh` ouvre un shell dans un conteneur en cours d'exécution, pour inspecter l'environnement en direct. On précise `-c` si le Pod comporte plusieurs conteneurs.",
    "t2-t10": "Si l'image ne contient pas d'outils de diagnostic, `kubectl debug` injecte un conteneur éphémère partageant les namespaces du Pod, sans le redéployer. On y embarque une image comme busybox pour investiguer réseau et système de fichiers.",
    "t2-t11": "Un Service qui ne route rien a généralement des Endpoints vides : soit son selector ne correspond à aucun Pod, soit les Pods ne sont pas Ready. On le confirme avec `kubectl get endpoints <svc>`.",
    "t2-t12": "Pour tester le DNS interne, on lance un Pod jetable (busybox:1.28) et on y exécute `nslookup <svc>.<ns>`. Cela vérifie que CoreDNS résout correctement les noms de Services.",
    "t2-t13": "Un Pod bloqué en ContainerCreating attend souvent un montage : Secret, ConfigMap ou PVC introuvable, ou un problème de réseau CNI. Le `describe pod` pointe l'erreur exacte dans ses Events.",
    "t2-t14": "Le kubelet étant un service systemd, ses logs se lisent avec `journalctl -u kubelet` (et son état avec `systemctl status kubelet`). C'est la source première pour diagnostiquer un nœud défaillant.",
    "t2-t15": "Les composants du control plane installés par kubeadm sont des Static Pods, dont les manifests vivent dans /etc/kubernetes/manifests. Une erreur de syntaxe dans l'un de ces fichiers fait disparaître le composant correspondant.",
    "t2-t16": "Un `describe node` expose des Conditions qui renseignent la santé du nœud : MemoryPressure, DiskPressure, PIDPressure et Ready. Une pression déclenche des évictions de Pods.",
    "t2-t17": "Un conteneur OOMKilled a dépassé sa limite mémoire et a été tué par le noyau (SIGKILL). On corrige en relevant la limite mémoire ou en traitant la fuite ; un dépassement CPU, lui, provoque du throttling, pas un kill.",
    "t2-t18": "`kubectl get pod <pod> -o yaml` affiche la ressource complète, spec et status compris. Utile pour lire finement le status (conditions, containerStatuses) au-delà de ce que montre `describe`.",
    "t2-t19": "`kubectl cp` copie des fichiers entre la machine locale et un conteneur, dans les deux sens. L'image doit contenir `tar`, car la copie s'appuie dessus.",
    "t2-t20": "Des Pods « Evicted » indiquent que le kubelet a libéré des ressources sous pression du nœud (mémoire ou disque). On confirme via les Conditions du nœud (MemoryPressure/DiskPressure).",
    "t2-t21": "Quand un Pod n'est pas placé, l'événement FailedScheduling du `describe pod` détaille pourquoi : ressources insuffisantes, taints non tolérés, ou contraintes d'affinité non satisfaites.",
    "t2-t22": "Une erreur x509 « certificate has expired » signale des certificats kubeadm périmés. On les renouvelle avec `kubeadm certs renew all`, puis on redémarre les Static Pods concernés ; `check-expiration` permet d'en contrôler les échéances.",
    "t2-t23": "`crictl` dialogue directement avec le runtime via l'interface CRI (ps, logs, inspect). Il est précieux pour déboguer un nœud quand l'API server ou le kubelet sont indisponibles.",
    "t2-t24": "Un Pod peut rester Pending parce que son PVC n'est pas lié (Pending). On vérifie alors qu'un PV compatible existe, ou qu'une StorageClass sait provisionner dynamiquement ; `describe pvc` explique l'échec du binding.",
    "t2-t25": "`kubectl rollout restart deployment/<nom>` recrée tous les Pods de façon progressive (rolling), sans coupure de service. C'est la bonne méthode pour, par exemple, forcer la relecture d'une configuration montée.",

    /* ---------- NETWORKING (theory.js) ---------- */
    "th-net-1": "ClusterIP est le type par défaut : le Service reçoit une IP virtuelle interne et stable qui répartit le trafic vers les Pods correspondant à son selector. Cette IP n'est joignable que depuis l'intérieur du cluster ; pour une exposition externe, on passe par NodePort ou LoadBalancer.",
    "th-net-2": "Un Service NodePort réserve un même port (dans la plage 30000–32767) sur tous les nœuds du cluster : une requête vers <IP_nœud>:<nodePort> est routée vers le ClusterIP sous-jacent, puis vers les Pods. C'est un moyen simple d'exposer un service à l'extérieur sans équilibreur cloud.",
    "th-net-3": "CoreDNS fournit le DNS interne du cluster : il permet de joindre un Service par un nom stable plutôt que par une IP volatile. Un Service `svc` du namespace `ns` se résout ainsi via svc.ns.svc.cluster.local. CoreDNS s'exécute dans kube-system.",
    "th-net-4": "Le modèle réseau de Kubernetes est « plat » : par défaut, tout Pod peut joindre tout autre Pod, sans restriction. Ce n'est qu'en créant une NetworkPolicy qui sélectionne des Pods que l'on bascule ceux-ci en refus par défaut, pour la direction (entrée et/ou sortie) qu'elle déclare.",
    "th-net-5": "Dès qu'une NetworkPolicy sélectionne des Pods et déclare le type Ingress, ces Pods n'acceptent plus que le trafic entrant explicitement autorisé par ses règles. Sans aucune règle ingress, tout l'entrant est bloqué : c'est le motif classique « default deny ingress ».",
    "th-net-6": "Un Ingress définit des règles de routage HTTP/HTTPS de niveau 7 (par hôte et par chemin) vers différents Services. Il n'agit pas seul : un Ingress Controller (nginx, traefik…) doit être déployé pour interpréter ces règles et acheminer réellement le trafic.",
    "th-net-7": "Un Service qui ne route rien a le plus souvent des Endpoints vides, parce que son selector ne correspond à aucun Pod prêt (labels différents ou Pods non Ready). Le diagnostic tient en une commande : `kubectl get endpoints <svc>` — s'il est vide, la piste est le selector ou la readiness.",
    "th-net-8": "Par défaut, les NodePort sont alloués dans la plage 30000–32767. Elle est configurable au niveau de l'API server via --service-node-port-range, mais il faut la connaître pour l'examen.",
    "th-net-9": "Un Service ne connaît pas ses Pods par leur nom : il utilise un label selector, et un contrôleur maintient à jour l'objet Endpoints/EndpointSlice avec les Pods correspondants. Un Service sans selector permet, à l'inverse, de gérer manuellement ses endpoints.",

    /* ---------- NETWORKING (theory2.js) ---------- */
    "t2-n1": "Sans champ `type`, un Service est de type ClusterIP : il obtient une IP interne stable qui load-balance vers ses Pods. NodePort et LoadBalancer sont des surcouches à demander explicitement pour une exposition externe.",
    "t2-n2": "Un Service headless (clusterIP: None) ne reçoit pas d'IP virtuelle : le DNS renvoie directement les adresses IP de ses Pods. C'est utile pour la découverte pair-à-pair, typiquement avec un StatefulSet.",
    "t2-n3": "Un Service ExternalName ne proxifie aucun trafic : il crée simplement un alias DNS (CNAME) vers un nom externe. Sans selector ni Endpoints, il sert à référencer un service hors cluster derrière un nom interne.",
    "t2-n4": "Dans un Service, `port` est le port exposé du Service tandis que `targetPort` est le port du conteneur des Pods. Le trafic suit donc la chaîne port (Service) → targetPort (conteneur).",
    "t2-n5": "Le nom DNS complet d'un Service suit le format <service>.<namespace>.svc.cluster.local. Depuis un Pod du même namespace, le nom court (ex. `api`) suffit souvent grâce au domaine de recherche.",
    "t2-n6": "Un objet Ingress ne décrit que des règles ; c'est l'Ingress Controller (nginx, traefik, HAProxy…) qui les applique réellement. Sans controller déployé, un Ingress reste sans effet.",
    "t2-n7": "Le TLS d'un Ingress s'appuie sur un Secret de type kubernetes.io/tls, qui contient les clés tls.crt et tls.key. L'Ingress y fait référence dans sa section `tls`.",
    "t2-n8": "Dans une NetworkPolicy, `policyTypes: [Egress]` contrôle le trafic sortant des Pods sélectionnés. On peut combiner Ingress et Egress dans une même policy pour filtrer les deux directions.",
    "t2-n9": "Les NetworkPolicies ne sont effectives que si le plugin CNI les implémente (Calico, Cilium…). Avec un CNI qui ne les supporte pas, les policies sont enregistrées mais ignorées — un piège fréquent.",
    "t2-n10": "kube-proxy programme l'accès aux Services en mode iptables ou ipvs. Le mode ipvs passe mieux à l'échelle lorsqu'il y a un grand nombre de Services.",
    "t2-n11": "`kubectl port-forward` établit un tunnel entre un port local et un Pod ou Service, pour un accès temporaire. C'est idéal pour tester une application sans l'exposer via un Service.",
    "t2-n12": "Les EndpointSlices ont remplacé l'objet Endpoints unique à grande échelle : ils découpent la liste des cibles en tranches, ce qui améliore performances et scalabilité sur les Services à nombreux Pods.",
    "t2-n13": "Une NetworkPolicy qui sélectionne des Pods avec le type Ingress mais sans aucune règle bloque tout le trafic entrant vers ces Pods. C'est le motif d'isolation « default deny ingress ».",
    "t2-n14": "La plage par défaut des NodePort est 30000–32767, ajustable via --service-node-port-range sur l'API server.",
    "t2-n15": "Pour vérifier qu'un Service a bien des cibles, `kubectl get endpoints <svc>` (ou get endpointslices) liste les Pods rattachés. Une liste vide signale un selector qui ne matche rien ou des Pods non Ready.",
    "t2-n16": "Les Pods CoreDNS s'exécutent dans le namespace kube-system, sous le label k8s-app=kube-dns. C'est là qu'on vérifie leur santé en cas de problème de résolution.",
    "t2-n17": "La communication Pod-à-Pod entre nœuds est assurée par le plugin réseau CNI (Calico, Cilium, Flannel…), qui implémente le réseau plat de Kubernetes. Sans CNI installé, les nœuds restent NotReady.",
    "t2-n18": "`kubectl expose deployment web --port=80` crée rapidement un Service qui reprend le selector du Deployment et cible ses Pods. C'est le raccourci pour rendre une application joignable sans écrire de YAML.",

    /* ---------- WORKLOADS (theory.js) ---------- */
    "th-work-1": "Un Deployment est l'abstraction standard pour les applications sans état : il gère des ReplicaSets, maintient le nombre de répliques voulu, et orchestre les mises à jour progressives (RollingUpdate) ainsi que les retours arrière. Un ReplicaSet seul garderait le compte, mais sans stratégie de mise à jour ni rollback.",
    "th-work-2": "Un DaemonSet garantit qu'une copie du Pod tourne sur chaque nœud — parfait pour des agents de logs, de monitoring ou de réseau. On peut restreindre l'ensemble des nœuds ciblés via un nodeSelector ou des affinités.",
    "th-work-3": "La stratégie RollingUpdate (par défaut d'un Deployment) remplace les Pods progressivement, en respectant maxSurge et maxUnavailable, de sorte que le service ne tombe jamais à zéro. La stratégie Recreate, elle, supprime tous les Pods avant d'en recréer, avec interruption.",
    "th-work-4": "Un taint NoSchedule posé sur un nœud repousse tous les Pods qui ne portent pas la toleration correspondante. Ainsi, sans rien faire, un Pod ordinaire est naturellement exclu des nœuds control-plane taintés ; ajouter une toleration l'y autoriserait au contraire.",
    "th-work-5": "nodeSelector exige une correspondance exacte de labels. nodeAffinity est plus expressif : il propose des règles obligatoires (required) ou préférentielles (preferred) et des opérateurs d'ensemble (In, NotIn…), permettant des placements souples impossibles avec nodeSelector.",
    "th-work-6": "Les ressources CPU s'expriment en millicœurs : 1000m équivaut à un vCPU, donc 500m correspond à un demi-cœur. Les requests servent au scheduling (réservation garantie) tandis que les limits plafonnent l'usage réel (throttling au-delà).",
    "th-work-7": "Pour injecter une valeur de ConfigMap en variable d'environnement, on utilise env.valueFrom.configMapKeyRef en précisant le nom du ConfigMap et la clé. Pour tout injecter d'un coup, envFrom.configMapRef expose l'ensemble des clés.",
    "th-work-8": "Un Job crée un ou plusieurs Pods et suit leur exécution jusqu'à atteindre le nombre de complétions demandé, puis s'arrête — idéal pour une tâche ponctuelle (batch, migration). Le CronJob ajoute une planification pour lancer des Jobs à intervalles réguliers.",
    "th-work-9": "`--dry-run=client -o yaml` demande à kubectl de produire le manifeste sans rien créer côté cluster. C'est le réflexe du CKA : générer un squelette avec `kubectl create ... --dry-run=client -o yaml > f.yaml`, puis l'éditer avant de l'appliquer.",

    /* ---------- WORKLOADS (theory2.js) ---------- */
    "t2-w1": "Le nombre de répliques voulu se déclare dans `spec.replicas` d'un Deployment. C'est ce champ que modifie `kubectl scale` ; `status.replicas`, lui, reflète l'état observé.",
    "t2-w2": "maxSurge et maxUnavailable paramètrent la stratégie RollingUpdate : le premier autorise des Pods en surplus temporaires, le second tolère des Pods indisponibles, ce qui règle la vitesse et la tolérance de la mise à jour.",
    "t2-w3": "`kubectl rollout pause deployment/<nom>` fige les mises à jour en cours, ce qui permet d'empiler plusieurs modifications puis de les diffuser d'un coup avec `rollout resume`.",
    "t2-w4": "Les initContainers s'exécutent l'un après l'autre, jusqu'à réussite, avant le démarrage des conteneurs applicatifs. Ils servent à préparer des prérequis : attendre une dépendance, initialiser un volume, récupérer un fichier.",
    "t2-w5": "La readinessProbe détermine si un conteneur est prêt à recevoir du trafic : tant qu'elle échoue, le Pod est retiré des Endpoints de ses Services. La liveness, elle, décide plutôt s'il faut redémarrer le conteneur.",
    "t2-w6": "Lorsque la livenessProbe échoue de façon répétée, le kubelet redémarre le conteneur (sans supprimer le Pod). C'est la différence clé avec la readiness, qui retire le Pod du trafic sans le redémarrer.",
    "t2-w7": "La startupProbe protège les conteneurs à démarrage lent : tant qu'elle n'a pas réussi, les sondes liveness et readiness sont suspendues. Cela évite qu'une liveness impatiente ne tue un conteneur qui met simplement du temps à démarrer.",
    "t2-w8": "Un StatefulSet convient aux applications qui ont besoin d'une identité stable : noms de Pods ordonnés et prévisibles (web-0, web-1…) et un PVC dédié par Pod. C'est le choix pour les bases de données, files et systèmes de stockage.",
    "t2-w9": "Pour injecter une clé de Secret en variable d'environnement, on utilise env.valueFrom.secretKeyRef (nom + clé). Pour injecter toutes les clés d'un coup, envFrom.secretRef fait le travail.",
    "t2-w10": "Renseigner `nodeName` dans un Pod l'affecte directement à ce nœud, en court-circuitant le scheduler. C'est un placement manuel, à réserver à des cas précis car il ignore ressources et contraintes.",
    "t2-w11": "Un HorizontalPodAutoscaler ajuste le nombre de répliques selon la charge, mais il a besoin du metrics-server pour lire l'usage CPU/mémoire via l'API metrics. Sans lui, le HPA ne dispose d'aucune donnée.",
    "t2-w12": "Un CronJob planifie la création de Jobs selon une expression cron à cinq champs. Chaque déclenchement lance un Job, qui exécute sa tâche jusqu'à complétion.",
    "t2-w13": "Dans un Job, `completions` fixe le nombre de Pods qui doivent réussir, tandis que `parallelism` règle combien s'exécutent simultanément. `backoffLimit` gère, lui, le nombre de tentatives en cas d'échec.",
    "t2-w14": "`resources.limits.cpu` plafonne le CPU qu'un conteneur peut consommer (au-delà, il est throttlé), alors que `resources.requests.cpu` réserve une quantité garantie utilisée par le scheduling.",
    "t2-w15": "Quand un Pod définit des requests égales à ses limits pour CPU et mémoire, il obtient la classe de qualité de service Guaranteed. Ces Pods sont les derniers évincés en cas de pression sur le nœud.",
    "t2-w16": "`kubectl rollout restart deployment/<nom>` recrée tous les Pods de manière progressive, sans changer le spec ni couper le service. Utile pour forcer la relecture d'une configuration ou d'un Secret monté.",
    "t2-w17": "Un ConfigMap est limité à environ 1 MiB, une contrainte héritée d'etcd. Pour des fichiers plus volumineux, mieux vaut passer par un volume dédié.",
    "t2-w18": "`nodeSelector` place un Pod sur les nœuds portant exactement les labels indiqués. C'est la forme la plus simple de contrainte de placement ; nodeAffinity en offre une version plus riche.",

    /* ---------- STORAGE (theory.js) ---------- */
    "th-sto-1": "Le PersistentVolumeClaim (PVC) est la demande de stockage formulée par un utilisateur ou un Pod : il précise la taille, le mode d'accès et éventuellement la StorageClass. Le PersistentVolume (PV) est la ressource réelle ; le PVC s'y lie (Bound) dès qu'un PV compatible existe.",
    "th-sto-2": "Une StorageClass décrit un type de stockage et le provisioner capable de le créer. Quand un PVC la référence, le PV est fabriqué automatiquement à la demande — c'est le provisionnement dynamique, qui évite de préparer les volumes à l'avance.",
    "th-sto-3": "Le mode ReadWriteOnce (RWO) autorise un montage en lecture-écriture par un seul nœud à la fois — le cas typique d'un disque block. ReadOnlyMany permet la lecture par plusieurs nœuds, ReadWriteMany l'écriture par plusieurs, et ReadWriteOncePod restreint à un unique Pod.",
    "th-sto-4": "La reclaimPolicy d'un PV décide de son sort à la suppression du PVC. Avec Delete, le PV et le stockage sous-jacent sont supprimés automatiquement. Avec Retain, le PV est conservé (état Released) pour permettre une récupération manuelle des données.",
    "th-sto-5": "Un volume emptyDir est créé au démarrage du Pod, partagé entre ses conteneurs, puis détruit quand le Pod disparaît. Il sert de stockage temporaire (cache, scratch) et ne survit pas au Pod, contrairement à un volume persistant.",
    "th-sto-6": "Un PVC reste Pending quand aucun PV ne satisfait sa demande (taille, mode d'accès, StorageClass) et qu'aucun provisionnement dynamique n'est disponible. On vérifie donc l'existence d'un PV compatible ou d'une StorageClass capable de créer le volume.",

    /* ---------- STORAGE (theory2.js) ---------- */
    "t2-s1": "Le binding d'un PVC à un PV repose sur la compatibilité : taille demandée, modes d'accès et storageClassName. Un PV trop petit ou d'une autre classe ne sera pas retenu, même disponible.",
    "t2-s2": "ReadWriteOnce (RWO) monte le volume en lecture-écriture sur un seul nœud à la fois. C'est le mode le plus courant, adapté aux disques block. ReadWriteOncePod va plus loin en le limitant à un seul Pod.",
    "t2-s3": "La reclaimPolicy Retain conserve le PV et ses données après suppression du PVC : le PV passe en Released et attend une intervention manuelle. Delete, à l'inverse, supprime le PV et le stockage réel.",
    "t2-s4": "Une StorageClass associe un provisioner à des paramètres de stockage : dès qu'un PVC la référence, un PV est créé dynamiquement. C'est ce qui permet d'obtenir du stockage à la demande sans préparation préalable.",
    "t2-s5": "emptyDir est un volume éphémère : créé avec le Pod, partagé entre ses conteneurs, et effacé à la suppression du Pod. Il est parfait pour un cache ou un espace de travail temporaire commun.",
    "t2-s6": "Un volume hostPath monte un fichier ou un dossier du système de fichiers du nœud dans le conteneur. Puissant mais à éviter en production : il lie le Pod à un nœud précis et pose des risques de sécurité.",
    "t2-s7": "Agrandir un PVC existant nécessite que sa StorageClass ait allowVolumeExpansion: true, et que le provisioner sous-jacent supporte l'expansion. On modifie ensuite la taille demandée dans le PVC.",
    "t2-s8": "Une StorageClass devient celle par défaut grâce à l'annotation storageclass.kubernetes.io/is-default-class réglée à « true ». Un PVC sans storageClassName utilise alors automatiquement cette classe.",
    "t2-s9": "En provisionnement statique, c'est l'administrateur qui crée les PV à l'avance ; les PVC viennent ensuite s'y lier. En dynamique, ce rôle est tenu par le provisioner de la StorageClass.",
    "t2-s10": "Un volume se déclare au niveau du Pod (section `volumes`), puis chaque conteneur le monte via `volumeMounts` en indiquant un mountPath. Le montage n'est donc pas automatique : il faut le déclarer côté conteneur.",
    "t2-s11": "Monté en volume, un ConfigMap expose chacune de ses clés comme un fichier dans le répertoire de montage. L'option subPath permet de n'y monter qu'une seule clé sans masquer le reste du dossier.",
    "t2-s12": "Avec la reclaimPolicy Retain, un PV passe à l'état Released après la suppression de son PVC : les données sont conservées mais le PV n'est pas réutilisable tel quel — il faut le nettoyer ou le recréer manuellement.",

    /* ---------- CLOUDNATIVEPG (theory2.js) ---------- */
    "t2-c1": "Le plugin kubectl `cnpg` simplifie l'exploitation d'un Cluster CloudNativePG : `kubectl cnpg status <cluster>` en affiche l'état complet (rôle des instances, réplication, santé). C'est l'outil de premier diagnostic.",
    "t2-c2": "CloudNativePG crée trois Services pour un Cluster : `-rw` (primaire, écritures), `-ro` (hot standby, lecture seule) et `-r` (n'importe quelle instance, lecture). Pour diriger des lectures vers les réplicas, on utilise donc `-ro`.",
    "t2-c3": "Une ressource `Backup` déclenche une sauvegarde à la demande d'un Cluster, tandis que `ScheduledBackup` planifie des sauvegardes récurrentes via un cron à six champs (secondes incluses).",
    "t2-c4": "Le bootstrap d'un Cluster CNPG suit une seule méthode par manifeste. `spec.bootstrap.initdb` — création d'un cluster PostgreSQL vide — est la méthode par défaut, aux côtés de recovery (restauration) et pg_basebackup (clonage).",
    "t2-c5": "Pour une restauration à un instant T (PITR), l'archive des WAL ne suffit pas : il faut aussi une base backup physique servant de point de départ. WAL + base backup permettent alors un RPO ≤ 5 minutes.",

    /* ---------- CLOUDNATIVEPG (cnpg.js) ---------- */
    "cn-th-1": "CloudNativePG pilote PostgreSQL de façon déclarative via la ressource personnalisée `Cluster`, qui décrit un primaire et d'éventuels réplicas. Toute la gestion — configuration, rôles, sauvegardes — passe par cette CRD et ses ressources associées.",
    "cn-th-2": "Un Cluster CNPG expose trois Services. `<cluster>-rw` route vers le primaire et sert donc les écritures ; `<cluster>-ro` dessert les réplicas en lecture seule, et `<cluster>-r` n'importe quelle instance en lecture.",
    "cn-th-3": "Contrairement à beaucoup d'opérateurs de bases de données, CloudNativePG n'utilise pas de StatefulSets : il gère directement les PersistentVolumeClaims des instances. Cela lui donne un contrôle plus fin sur le cycle de vie des volumes et les basculements.",
    "cn-th-4": "Un Cluster s'amorce via spec.bootstrap avec une seule méthode : initdb pour créer un cluster vide, recovery pour restaurer depuis une sauvegarde (PITR possible), ou pg_basebackup pour cloner un cluster existant de même version majeure.",
    "cn-th-5": "La restauration à un instant T combine l'archive continue des WAL et une base backup physique : « un WAL archive seul est inutile ». Sans base backup, il n'existe aucun point de départ pour rejouer les WAL.",
    "cn-th-6": "CloudNativePG expose ses métriques au format Prometheus sur le port 9187 — le port conventionnel d'un exporter PostgreSQL. Cela permet de superviser l'état du cluster et de la réplication.",
    "cn-th-7": "Deux ressources gèrent les sauvegardes : `Backup` pour une sauvegarde ponctuelle et `ScheduledBackup` pour des sauvegardes planifiées via un cron à six champs. Elles précisent le cluster cible et la méthode (plugin ou volumeSnapshot).",
    "cn-th-8": "CloudNativePG s'appuie sur la réplication streaming native de PostgreSQL et supporte le mode synchrone en deux variantes : basée sur le quorum et basée sur la priorité. La réplication se fait au niveau applicatif, pas au niveau du stockage.",
    "cn-th-9": "CloudNativePG, créé à l'origine par EDB, est un projet open-source distribué sous licence Apache 2.0. Il évolue aujourd'hui sous la gouvernance de la CNCF.",
    "cn-th-10": "Au sein d'un même cluster Kubernetes, la perte du primaire déclenche un failover automatique : CloudNativePG promeut une autre instance et fait pointer le service `-rw` dessus, sans intervention. En topologie multi-cluster (replica cluster), la promotion reste en revanche manuelle.",
  };

  let n = 0;
  (window.CKA.questions || []).forEach((q) => { if (RICH[q.id]) { q.explain = RICH[q.id]; n++; } });
  window.CKA._richExplain = n;
})();
